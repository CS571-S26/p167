import WorldMap from '../WorldMap';

const MapPage = () => {
  return (
    <div className="map-page-wrapper">
      {/* You can add a title or sidebar here too */}
      <WorldMap />
    </div>
  )};

export default MapPage;